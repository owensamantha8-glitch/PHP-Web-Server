# PHP-Web-Server
PHP is a popular general scripting programming language geared towards development. 

Runtime configuration is environment-driven via `bootstrap.php`:
- `LUM_APP_ROOT` (or `LUM_ROOT`) for the application root path
- `LUM_APP_URL` for absolute web URLs (for example login redirects)
- `LUM_DB_CONFIG` for the external database INI file path
